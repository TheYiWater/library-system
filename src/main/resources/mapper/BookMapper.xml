<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.librarysystem.mapper.BookMapper">

    <select id="selectById" resultType="com.example.librarysystem.entity.Book">
        SELECT id, title, author, isbn, category_id, publisher, stock, total,
        create_time, update_time
        FROM book
        WHERE id = #{id}
    </select>

    <select id="selectByCondition" resultType="com.example.librarysystem.entity.Book">
        SELECT * FROM book
        <where>
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="author != null and author != ''">
                AND author LIKE CONCAT('%', #{author}, '%')
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <insert id="insert" parameterType="com.example.librarysystem.entity.Book" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO book(title, author, isbn, category_id, publisher, stock, total)
        VALUES(#{title}, #{author}, #{isbn}, #{categoryId}, #{publisher}, #{stock}, #{total})
    </insert>

    <update id="updateStock">
        UPDATE book
        SET stock = stock + #{delta}
        WHERE id = #{id} AND stock + #{delta} >= 0
    </update>

    <delete id="deleteById">
        DELETE FROM book WHERE id = #{id}
    </delete>
</mapper>