<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.librarysystem.mapper.BorrowRecordMapper">

    <resultMap id="BaseResultMap" type="com.example.librarysystem.entity.BorrowRecord">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="book_id" property="bookId"/>
        <result column="borrow_time" property="borrowTime"/>
        <result column="due_time" property="dueTime"/>
        <result column="return_time" property="returnTime"/>
        <result column="status" property="status"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, user_id, book_id, borrow_time, due_time, return_time, status
        FROM borrow_record
        WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT id, user_id, book_id, borrow_time, due_time, return_time, status
        FROM borrow_record
        WHERE user_id = #{userId}
        ORDER BY borrow_time DESC
    </select>

    <select id="selectByBookId" resultMap="BaseResultMap">
        SELECT id, user_id, book_id, borrow_time, due_time, return_time, status
        FROM borrow_record
        WHERE book_id = #{bookId}
        ORDER BY borrow_time DESC
    </select>

    <insert id="insert" parameterType="com.example.librarysystem.entity.BorrowRecord" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO borrow_record(user_id, book_id, due_time, status)
        VALUES(#{userId}, #{bookId}, #{dueTime}, #{status})
    </insert>

    <update id="markReturned">
        UPDATE borrow_record
        SET status = 1, return_time = NOW()
        WHERE id = #{id} AND user_id = #{userId} AND status = 0
    </update>
</mapper>